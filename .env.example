# Configuration de base de données
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=examenphp_dev

# Configuration de l'application
APP_ENV=development
APP_SECRET_KEY=changez-moi-avec-une-clé-aléatoire-64-caractères

# Configuration email (si utilisé)
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_FROM=dev@localhost
